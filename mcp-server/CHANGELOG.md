## [1.3.4] - 2026-03-27

### 修复

- 工具 schema 移除所有 `additionalProperties: false` 约束，解决 MiniMax 等部分模型在工具调用时返回格式异常的兼容性问题。

---

## [1.3.3] - 2026-03-25

### 变更

- `schematic_check` 工具拆分为两个专用工具：`schematic_topology`（ERC + 器件拓扑提取，为自动连线准备数据）和 `schematic_netlist`（完整网表提取，供 AI 功能性分析）。
- 内置 AI 助手指令中，原理图检查流程描述由“器件布局”语义调整为“原理图拓扑快照”语义，减少功能性检查误判。

---

## [1.3.2] - 2026-03-25

### 优化

- 工具定义中的 `description` 调整为简短功能介绍，避免在工具描述中重复展开调用约束。
- 已暴露工具的调用约束统一写入内置 AI 助手指令，集中约束 `schematic_check`、`component_select`、`component_place` 的使用方式。

---

## [1.3.1] - 2026-03-25

### 新增

- 新增 `api_index` 工具实现，获取原理图常用可调用 API 索引表，每项包含完整路径与功能描述，作为 API 调用流程第一步。当前暂未对模型暴露，后续按需启用。

### 优化

- 工具优先级规则：`schematic_check`、`component_select`、`component_place` 为高优先级专用工具，四个 API 通用工具为托底工具，仅在专用工具均无法满足需求时才允许使用。
- 新增 API 调用三步顺序强制规则：必须先用 `api_index` 获取索引，再用 `api_search` 确认签名，最后才能用 `api_invoke` 执行调用，禁止跳过任意步骤。
- 工具定义补充完整约束，包括 API 路径格式说明、scope 枚举限制、电源地符号禁止自动放置等。

---

## [1.3.0] - 2026-03-24

### 新增

- 新增器件交互选型能力：AI 检索到候选器件后，可在 VS Code / Cursor 侧边栏以表格形式等待用户确认具体型号。
- 新增器件交互放置能力：AI 可按顺序驱动用户在原理图中逐个放置器件，并在侧边栏显示当前进度。

### 变更

- 器件选型与器件放置过程中的取消操作改为仅跳过当前器件，后续器件继续处理，不再整体中断流程。
- 电源符号和地符号不再由 AI 自动放置，改为明确提示用户手动添加。
- 开启“打开 EDA 时关闭侧边栏”后，除了打开 EDA 时自动收起外，器件选型或器件放置完成后也会自动收起侧边栏。- `jlceda_schematic_check` 返回结果从精简网表（`netlist`）改为器件布局图（`componentLayout`），包含每个器件的位号、坐标、旋转、镜像、封装，以及每个引脚的名称、编号、类型、坐标、旋转、引脚长度、连接状态。
---

## [1.2.0] - 2026-03-21

### 新增

- 支持原理图完整检查能力，AI 可以直接发起检查并拿到结果。

### 优化

- 发起操作前的最长等待时间调整为 30 秒，启动较慢时成功率更高。
- 状态提示文案整理为更易理解的表达，日常查看更直观。

### 修复

- 首次聊天时若连接尚未完全建立，系统会先等待就绪再执行，减少首轮报错。
- 修复部分状态提示不一致问题，显示更稳定。

---

## [1.1.3] - 2026-03-20

### 变更

- 原理图功能性审查速度更快，AI 能更高效地获取电路全貌。

---

## [1.1.2] - 2026-03-19

### 变更

- 用户自定义指令由覆盖模式改为追加模式：内置指令始终有效，用户自定义指令在其基础上叠加生效，不再互相冲突。

---

## [1.1.1] - 2026-03-19

### 新增

- EDA 连接器与 MCP 服务端版本不一致时，在 VS Code 右下角弹出提示，告知哪一侧版本较低并建议升级。
- 系统日志：字段开关区域默认折叠，通过字段按钮切换显示，界面更简洁。
- 系统日志：连接/断开日志现在正确显示触发变化的客户端 ID 以及当前活动客户端 ID。
- 侧边栏各卡片（桥接配置、AI 指令、系统日志、连接列表、调试控制）的折叠/展开状态自动保存，重新打开后恢复上次状态。
- 修复：调试控制卡片默认未折叠的问题。

---

## [1.1.0] - 2026-03-18

### 新增

- 侧边栏底部新增打开 EDA 时自动收起侧边栏的开关，可在 VS Code 设置中配置。

### 变更

- 优化：AI 在原理图中放置器件时，会自动将器件布局在图纸中心区域，不再出现摆到边角或超出红框的情况。
- 优化：AI 放置完器件后会自动完成所有引脚的电气连接，不再留悬空引脚要用户手动补线。
- 新增：用户说检查原理图时，AI 默认进行功能性审查（分析电路用途、器件选型、引脚连接、能否正常工作），只有明确说跑 ERC 或做 DRC 时才执行规则检查。

---

## [1.0.0] - 2026-03-16

### 当前功能

**多 AI 客户端支持**

- 支持在 VS Code（GitHub Copilot）中作为 MCP 服务使用。
- 支持在 Cursor 中作为 MCP 服务使用。
- 宿主类型自动识别，无需手动切换。

**侧边栏面板**

- 在 VS Code 活动栏注册「嘉立创 EDA MCP」侧边栏，实时显示服务运行状态和桥接连接状态。
- 显示已连接的 EDA 实例列表，区分活动与待命角色。

**三个 MCP 工具**

- api_search：供 AI 检索嘉立创 EDA API 文档。
- api_invoke：供 AI 调用嘉立创 EDA API，执行实际操作。
- eda_context：供 AI 获取当前 EDA 运行时状态（当前工程、文档、图页、选中元件等）。

**服务配置**

- 桥接监听地址和端口通过 VS Code 设置持久化，配置变更后自动重新对接 AI 客户端。

**AI 助手指令**

- 内置嘉立创 EDA 操作系统指令，AI 连接时自动获取。
- 支持在 VS Code 设置中自定义补充指令，保存后重连即生效。
