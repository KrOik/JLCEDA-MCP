## [1.5.0] - 2026-04-01

### 新增

- 器件选型面板弹出时显示超时倒计时，最后 1 分钟自动标红，用户可随时了解剩余操作时间。

### 优化

- 器件选型面板的选中行高亮颜色改用 VS Code 标准列表选中色，深色主题下文字对比度更佳，不再出现背景与文字难以区分的问题。

### 变更

- 版本与服务端 `1.5.0` 保持同步。
- 服务端（VS Code 扩展）显示名由「嘉立创 EDA MCP」更名为「JLCEDA MCP」，扩展 ID 由 `chengbin.jlceda-mcp-server` 更名为 `chengbin.jlceda-mcp`；本扩展文档中的名称引用与 Marketplace 链接已同步修正。

---

## [1.4.1] - 2026-03-31

### 新增

- 新增 `/bridge/jlceda/schematic/review` 桥接路由，对应 `schematic_review` 工具，在 EDA 扩展侧执行 `sch_ManufactureData.getNetlistFile` 读取全工程网表文件。

---

## [1.4.0] - 2026-03-31

### 变更

- 扩展名称由「MCP 连接器」更名为「MCP Bridge」，菜单入口、连接设置页与状态说明同步更新。
- `schematic_topology` 与 `schematic_netlist` 两个工具合并为 `schematic_read`，输出以电路语义为核心的结构化 JSON，包含器件列表、引脚→网络名映射与网络连接关系；数据来自 EDA 实时内存状态，不依赖网表文件生成，刚放置或修改的器件立即可见。

---

## [1.3.4] - 2026-03-27

### 变更

- 无独立 Bridge 侧功能变更，本版本用于与 MCP 服务端 `1.3.4` 保持版本同步。

---

## [1.3.3] - 2026-03-25

### 变更

- `jlceda_schematic_check` 工具拆分为两个专用工具：`schematic_topology`（ERC + 器件拓扑提取，为自动连线准备数据）和 `schematic_netlist`（完整网表提取，供 AI 功能性分析）。
- `jlceda_schematic_check` 返回字段由 `componentLayout` 更名为 `schematicTopology`，避免将功能性检查误解为布局检查。
- 原理图检查相关说明统一改为“原理图拓扑快照”表述，强调连接关系与结构分析语义。

---

## [1.3.2] - 2026-03-25

### 变更

- 无独立 Bridge 侧功能变更，本版本用于与 MCP 服务端 `1.3.2` 保持版本同步。

---

## [1.3.1] - 2026-03-25

### 新增

- 新增 `/bridge/jlceda/api/index` 桥接路由，对应 `api_index` 工具，提供原理图常用可调用 API 静态索引，支持按命名空间过滤；该工具当前暂未对模型暴露，后续按需启用。

---

## [1.3.0] - 2026-03-24

### 新增

- 新增器件交互选型支持：当 AI 检索到候选器件后，可在 VS Code / Cursor 侧边栏以表格形式等待用户确认具体型号。
- 新增器件交互放置支持：AI 可按顺序驱动用户在原理图中逐个放置器件，并在侧边栏显示当前进度。

### 变更

- 器件选型与器件放置过程中的取消操作改为仅跳过当前器件，后续器件继续处理，不再整体中断流程。
- 电源符号和地符号不再由 AI 自动放置，改为明确提示用户手动添加。
- 开启“打开 EDA 时关闭侧边栏”后，除了打开 EDA 时自动收起外，器件选型或器件放置完成后也会自动收起侧边栏。- `jlceda_schematic_check` 返回结果从精简网表（`netlist`）改为器件布局图（`componentLayout`），包含每个器件的位号、坐标、旋转、镜像、封装，以及每个引脚的名称、编号、类型、坐标、旋转、引脚长度、连接状态。

---

## [1.2.0] - 2026-03-21

### 新增

- 现在可以直接让 AI 对原理图做一次完整检查，并返回更精简、更容易阅读的结果。

### 优化

- 启动等待时间延长到 30 秒，软件刚启动或电脑较慢时也更容易连上。
- 服务尚未启动时，状态提示会明确显示正在等待启动，不再用容易误解的超时提示。
- 连接状态提示按使用场景重新整理，页面展示更清楚。

### 修复

- 首次打开聊天时，如果连接还在建立，会先等待连接完成再执行操作，减少首次报错。
- 修复部分状态提示偶发显示不准确的问题。

---

## [1.1.3] - 2026-03-20

### 变更

- 原理图功能性审查速度更快，AI 能更高效地获取电路全貌。
- ERC/DRC 检查结果判断更准确，不再出现检查已通过但仍反复尝试获取详细结果的情况。
- AI 进行功能性审查时不会生成多余的文件下载链接。

---

## [1.1.2] - 2026-03-19

### 变更

- 服务端用户自定义指令由覆盖模式改为追加模式：内置指令始终有效，用户自定义指令在其基础上叠加生效，不再互相冲突。

---

## [1.1.1] - 2026-03-19

### 新增

- EDA Bridge 与 MCP 服务端版本不一致时，在 VS Code 右下角弹出提示，告知哪一侧版本较低并建议升级。

---

## [1.1.0] - 2026-03-18

### 新增

- 侧边栏底部新增打开 EDA 时自动收起侧边栏的开关，避免启动 EDA 后侧边栏遮挡视野。

### 变更

- 优化：AI 在原理图中放置器件时，会自动将器件布局在图纸中心区域，不再出现摆到边角或超出红框的情况。
- 优化：AI 放置完器件后会自动完成所有引脚的电气连接，不再留悬空引脚要用户手动补线。
- 新增：用户说检查原理图时，AI 默认进行功能性审查（分析电路用途、器件选型、引脚连接、能否正常工作），只有明确说跑 ERC 或做 DRC 时才执行规则检查。

---

## [1.0.0] - 2026-03-16

### 当前功能

**安装与激活**

- 扩展安装后随嘉立创 EDA 启动自动激活，无需手动触发。
- 在首页、原理图、PCB 顶部菜单栏注入「MCP Bridge」菜单，提供「连接设置」和「关于」入口。

**连接设置**

- 支持配置桥接服务地址，地址格式实时校验，非法格式拒绝保存并提示错误。
- 设置页实时显示桥接连接状态和 WebSocket 连接状态。
- 配置变更后自动重新连接，无需重启 EDA。

**桥接连接**

- 激活后自动连接到 MCP 服务端，断线后自动重连。
- 支持多个 EDA 实例同时接入，由服务端自动裁决哪个实例执行任务。
- 当前页面不是原理图或 PCB 编辑页时，连接自动暂停。

**EDA 工具支持**

- 支持 API 检索、API 调用、当前上下文获取三个 AI 工具，AI 通过这三个工具操控嘉立创 EDA。
